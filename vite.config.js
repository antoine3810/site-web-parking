import { defineConfig } from "vite";

// base: './' garde des chemins d'assets relatifs (utile pour héberger le
// build depuis n'importe quel sous-dossier, ou pour une prévisualisation
// statique packagée).
export default defineConfig({
  base: "./",
});
